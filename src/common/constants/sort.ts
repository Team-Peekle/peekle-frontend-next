import { SortOptions } from '@common/types//sort';

export const SORT_LABELS = {
  [SortOptions.NEAREST_DATE]: '가까운 날짜순',
  [SortOptions.LOWEST_PRICE]: '낮은 가격순',
  [SortOptions.NEAREST_DISTANCE]: '가까운 거리순',
};
